[package]
name = "hc-workflow"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
regex = "1.12"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
sha2 = "0.10"
thiserror = "1.0"
time = { version = "0.3", features = ["formatting"] }
