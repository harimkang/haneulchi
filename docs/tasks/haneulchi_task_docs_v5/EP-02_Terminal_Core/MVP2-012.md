# MVP2-012 — copy/paste/find/scrollback/hyperlink 등 terminal 기본 UX 구현
> Scope: **MVP**  
> Epic: **EP-02 / Terminal Core**  
> Plan Mode: **Sprint 2**

## 1. Task Summary
| Field | Value |
|---|---|
| Task ID | MVP2-012 |
| Story | copy/paste/find/scrollback/hyperlink 등 terminal 기본 UX 구현 |
| Priority | Must |
| Size | M |
| User Cases | UC-03 — daily terminal navigation / UC-04 — run generic CLI/TUI tools |
| Screen Refs | Project Focus / Terminal Deck |
| PRD Refs | FR-02 |
| Depends On | MVP2-009 |
| Exit Hook | AC-03, AC-04 |
| Suggested Owner Lane | Runtime/Core |

## 2. Objective
이 task는 **copy/paste/find/scrollback/hyperlink 등 terminal 기본 UX 구현** 를 실제 구현 단위로 분해해, Terminal Core의 결과를 제품 화면과 수용 기준에 연결한다.

## 3. Why This Task Exists
- 이 task는 **Terminal Core** 의 outcome을 실제 제품 기능으로 내린다.
- 관련 사용자는 UC-03 — daily terminal navigation / UC-04 — run generic CLI/TUI tools 흐름을 통해 이 기능을 직접 체감한다.
- 완료되면 `WF-02` 화면군에서 operator-visible 동작이 닫혀야 한다.

## 4. In Scope
- `copy/paste/find/scrollback/hyperlink 등 terminal 기본 UX 구현` 에 직접 필요한 UI, 상태, 액션, 데이터 연결
- `Project Focus / Terminal Deck` 화면에서 사용자 흐름이 닫히도록 만드는 최소 구현
- `AC-03, AC-04` 또는 해당 epic gate에 필요한 검증 훅
- state/event/projection/diagnostics 정합성 유지

## 5. Out of Scope
- 별도 P1/P2로 밀린 확장 항목
- unrelated screen redesign
- raw terminal parsing에 의존한 state 추정 확대
- full browser / multi-window / external sync 같은 비핵심 확장

## 6. Implementation Breakdown
- T1. copy/paste/find/scrollback/hyperlink 등 terminal 기본 UX 구현 범위를 domain/state/API 단위로 분리
- T2. Project Focus / Terminal Deck 화면에서 필요한 view-model / interaction contract 연결
- T3. persistence / event / projection 업데이트
- T4. failure / degraded / empty 상태 정의
- T5. demo 증적과 regression 체크 정리


## 7. Data / Interface Notes
- 주요 모듈 후보: runtime-core lane
- 상태 source of truth는 UI 추정이 아니라 runtime / persistence / workflow / snapshot 순으로 유지한다.
- screen contract는 stable vocabulary(manual, claimed, retry_due, workflow_ok 등)를 재사용한다.

## 8. QA Checklist
- `copy/paste/find/scrollback/hyperlink 등 terminal 기본 UX 구현` 의 happy path가 `Project Focus / Terminal Deck` 에서 재현된다.
- 의존성 `MVP2-009` 이후에 독립 데모가 가능하다.
- `AC-03, AC-04` 와 연결되는 최소 증적을 남긴다.
- degraded / error / empty 경로가 누락되지 않는다.

## 9. Done Definition
- story `copy/paste/find/scrollback/hyperlink 등 terminal 기본 UX 구현` 를 위한 코드/화면/서비스가 연결되어 있다.
- 관련 화면 `Project Focus / Terminal Deck` 에서 empty/error/degraded까지 포함해 동작을 확인했다.
- `AC-03, AC-04` 또는 해당 epic gate에 필요한 체크가 통과했다.
- 다음 sprint/task가 이어 받을 수 있는 상태 스냅샷과 demo 노트가 남아 있다.

## 10. Handoff Notes
- 새 범위가 생기면 backlog와 PRD/Wireframe trace를 먼저 확인한다.
- 공통 vocabulary를 새로 만들기보다 existing state words를 재사용한다.
- terminal-first 원칙을 깨지 않는지 마지막에 다시 검토한다.

## 11. Current Implementation Status
- `2026-03-21` 기준 main에 live renderer IO forwarding, incremental feed path, scrollback smoke coverage, repeatable smoke script가 반영됐다.
- scrollback retention 검증과 hyperlink / paste / basic live-session UX regression은 자동 smoke에 포함됐다.
- explicit first-responder handoff와 optional action-strip polish는 아직 남아 있으므로, 이 task는 substantial implementation 상태이며 final closure 전 추가 마감이 필요하다.
